# Negative control for check_spec_isolation — failure case 12

Every log file the carve-out names, travelling with implementation code. This
**must pass**, and the requirement is not a courtesy: `HANDOFF.md` is *required*
to change alongside the code it describes, and `DIVERGENCES.md` and
`DEFERRALS.md` are written during the work they record.

A checker that treats logs as spec makes the session-end ritual unmergeable, at
which point the ritual stops happening. The carve-out is what keeps the ledger
honest, so it is tested as carefully as the prohibition is.
