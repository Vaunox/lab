"""Certifying test named by the negative control's single manifest row."""


def test_clean_row_is_present() -> None:
    """Named by P9.CLEAN.PRESENT so the row's evidence citation resolves."""
    return None
